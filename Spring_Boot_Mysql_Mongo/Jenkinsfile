pipeline {
    agent any
    tools {
        maven 'maven' 
    }
    stages {
        stage('clean') {
            steps {
                sh 'mvn --version'
                sh 'cd Spring_Boot_Mysql_Mongo && mvn clean'
            }
        } 
     stage('Quality Analysis') {
      parallel {
        stage('Integration Test') {
          agent any
          steps {
            echo 'Run integration tests here...'
          }
        }
      }
       }
         stage('build') {
            steps {
                sh 'mvn --version'
                sh 'cd Spring_Boot_Mysql_Mongo && mvn clean install -Dmaven.test.skip=true'
            }
        }
    }
}
